% abstract.tex -- three-part paradigm (skills/paper-playbook/sections/02_abstract.md).
% Every number: % EV:<id>. Keep title + abstract + keywords on page 1.
\begin{abstract}
\setlength{\parindent}{2em}
% (1) State of the art: consensus -> bottleneck -> routes (venue'year) -> open question.
<<Field consensus sentence.>> <<Bottleneck sentence.>>
Existing work reduces <<cost>> either by <<route 1>>~\cite{Gentry09} (<<Venue'YY>>)
or by <<route 2>>~\cite{ToyEprint} (<<Venue'YY>>).
However, whether <<the two can be combined / X can be lowered further>> has remained open.

\setlength{\parindent}{2em}
% (2) Our technique: viewpoint -> one sentence per contribution (same order as 1.2).
We present <<Name>>, which views <<object>> as <<structure>>.
<<Structure/bound sentence.>> <<Placement of prior work: "the methods of [A] and [B]
are the cases ... of our construction".>> <<Algorithm + cost formula sentence.>>

\setlength{\parindent}{2em}
% (3) Results: scope -> range -> concrete points -> artifact.
On <<N>> instances at $\ge$<<lambda>>-bit estimated security, <<stage>> is
<<a>>--<<b>>$\times$ faster than <<strongest baseline>> in the same library
(e.g.\ <<x>>\,ms $\to$ <<y>>\,ms). % EV:<<id>>
Our implementation is publicly available.\footnote{<<anonymous link during review>>}

\keywords{<<Keyword 1>> \and <<Keyword 2>> \and <<Keyword 3>>}
\end{abstract}
