# CLAIMS — falsifiable statements (TEACHING EXAMPLE)

> Rule: every claim that may appear in the paper is registered here **before** it is written.
> The `falsifier` attacks `open` claims first. Only `survived` (or `weakened` with the
> weakened wording) claims may enter `paper/`. Refuted claims stay in the table — they are data.
> Scripts: `falsify/<claim>/…py`, outputs next to them as `.log`.

| ID | claim (precise, falsifiable, with parameter range) | proposer | status | falsifier evidence / counterexample | EVIDENCE refs (+ THEORY) |
|---|---|---|---|---|---|
| C1 | **Correctness.** At toy set T1, MV-PBS with the factorisation T_i = v0·d_i decrypts every output bit of the PRESENT S-box correctly, for all 16 inputs; the factorisation is an exact polynomial identity for every LUT Z_p → Z_p. Kill: one wrong output bit, or one LUT where v0·d_f ≠ T_f. | idea-miner | survived | Could not refute. `falsify/C1_correctness/attack_correctness.py`: 3 key seeds × 16 inputs × 4 bits = 192/192 correct; edge LUTs (constant 0/1, the max-norm LUT with ‖d‖² = 18, alternating) 64/64 correct in one MV-PBS call. Identity L1 checked on 66 564 LUTs (theory log). | E16, E17, THEORY L1 |
| C2 | *Original:* "MV-PBS evaluates all 4 output bits with **one** blind rotation **with the same correctness as per-bit PBS**." *Adopted weakened wording:* MV-PBS evaluates all k ≤ 4 output bits with one blind rotation; the variance of output i is ‖d_i‖²·Var_BR + Var_KS instead of Var_BR + Var_KS (‖d_i‖² = 10, 10, 8, 8 for PRESENT, ≤ 18 for any Boolean LUT), so correctness matches per-bit PBS **only if this fits the decoding margin**: no failure among 384 outputs at T1, but at the noisy set T2 the S-box failure rate rises from 0/128 (per-bit) to 32/128 at k = 4. | idea-miner | weakened | `falsify/C2_mv_noise/refute_equal_correctness.py` on results/noise_T2: MV-PBS failure Wilson CI lies strictly above the per-bit CI for every k = 1..4 (k=4: [0.183, 0.332] vs [0, 0.029]). Measured std of every output (both methods) matches Theorem T1 within 0.2 bits. | E10, E11, E12, E13, E14, E15, E16, THEORY T1 |
| C3 | "Since MV-PBS replaces k blind rotations by one, its speed-up over per-bit PBS is **≥ 0.9·k for every k ∈ {1,…,64}**." Kill: CI upper end < 0.9k for some k. | pi-orchestrator | refuted | Proposer measured only k ≤ 4 (all consistent). Falsifier went to the edge of the claimed range: k = 64 gives 43.67× [42.53, 44.63] < 57.6 (`falsify/C3_linear_speedup/check_linear_speedup.py`, 5 interleaved rounds). Per-LUT post-processing does not amortise (Amdahl). Replaced by C6. | E9 |
| C4 | **Measured speed-up.** At T1, for the full 4-bit S-box (k = 4, 16 inputs), the median speed-up of MV-PBS over per-bit PBS (process CPU time, same keys, interleaved) lies in [3, 4). Kill: 95% CI leaves [3, 4) or the arms differ in configuration. | experimenter | survived | Could not refute. `falsify/C4_speedup_recompute/recompute.py` re-parses all raw per-execution logs (not summary.log): same command line up to `--method`, all 64/64 output bits correct in every run, 3.86× [3.79, 3.95]. | E1, E2, E3 |
| C5 | "The CMux-tree / vertical-packing S-box (`vpack`) is **≥ 5× faster than MV-PBS**." Kill: an unfair comparison or an end-to-end cost above MV-PBS. | idea-miner | refuted | Raw ratio (7.81×) is real but compares different input formats: vpack consumes 4 GGSW-encrypted bits. From one LWE nibble the pipeline needs bit extraction (≥ 1 blind rotation) + 4 circuit bootstraps (≥ ℓ_cb PBS each). Even with ℓ_cb = 1 the end-to-end lower bound is ≥ 376 ms, i.e. ≤ 0.20× of MV-PBS speed (`falsify/C5_vpack_fairness/cost_end_to_end.py`). | E4, E19 |
| C6 | **Saturation (replaces C3).** At T1 the speed-up of MV-PBS over per-bit PBS is sub-linear in the number of LUTs: at k = 64 it is below 0.9·64. Kill: CI upper end ≥ 57.6. | falsifier | survived | Raw per-execution logs of results/falsify_C3_k64 re-parsed (not summary.log); 43.67× [42.53, 44.63]. | E9 |

Status ∈ {open, survived, weakened, refuted}.
