# DECISIONS — dated go/no-go log (TEACHING EXAMPLE; dates are illustrative)

| date | decision | options considered | evidence | decided by | revisit when |
|---|---|---|---|---|---|
| 2026-09-14 | **D-1 Scope.** Study only the case where the 4 output *bits* of the S-box are needed as separate ciphertexts (bit-sliced next layer). Toy, insecure parameters only; no security estimation. | (a) nibble output: a single PBS with f = S already gives S(x) in Z_16 — nothing to optimise; (b) bit outputs: 4 PBS vs MV-PBS vs CMux tree | lit-scout note in LITERATURE.md (CJP21 programmable bootstrapping); cost screen in IDEAS I1 | pi-orchestrator | never (teaching scope) |
| 2026-09-15 | **D-2 Waive the "≥ 15 works" P1 gate item.** 8 verified public works are enough for a teaching toy. | full sweep with workflows/lit-sweep.js vs. manual list of classics | bib-verify-report.md: 8/8 VERIFIED | pi-orchestrator | if the toy is ever turned into a real paper |
| 2026-09-18 | **D-3 Add a second, deliberately noisy parameter set T2** (GLWE σ = 2^-22 instead of 2^-28) so that the noise claim C2 can actually be attacked; at T1 the key-switch noise hides the MV-PBS amplification. | (a) only T1 → C2 untestable; (b) T2 chosen from Theorem T1 so that per-bit PBS still has ≈ 2^-26 failure probability per bit while MV-PBS does not | theory/check_mvpbs_noise.log (T1b prediction) | theorist + falsifier, approved by PI | – |
| 2026-09-25 | **D-4 Timing metric = process CPU time, not wall time.** First P5 run on the shared workstation (load average up to 70 on 104 CPUs) gave per-bit PBS IQR 257 ms on a 383 ms median and a k=4 speed-up CI of [3.10, 6.25]; the re-run with `time.process_time` (single-threaded code) gives IQR 0.5 ms and CI [3.79, 3.95]. Wall time is still logged next to every sample. | wall time with more rounds (exceeds 5-min budget); pinning (`--cpus`) onto an unknown-load core; CPU time | first-run logs kept outside the repo (superseded); results/bench_T1_k4/summary.log | experimenter, approved by PI | if code becomes multi-threaded (CPU time would then over-count) |
| 2026-09-25 | **D-5 P6 verdicts accepted; go to P7.** C3 ("speed-up ≥ 0.9k for all k ≤ 64") refuted at k = 64 → replaced by C6 (saturation). C5 (vpack ≥ 5× faster) refuted on input-format fairness → vpack appears in the paper only as a remark without a speed-up number. C2 adopted in its weakened wording. | re-scope to k ≤ 4 only (hides the Amdahl effect — rejected); keep vpack as a baseline (unfair — rejected) | falsify/*/…log; CLAIMS.md | pi-orchestrator | – |
| 2026-09-25 | **D-6 Mock review R1 → back to P7 once.** Add the Amdahl/saturation sentence, state that timings are CPU time of pure Python, and say explicitly that one PBS with a nibble output would do if bits are not needed. | ignore (reviewer-sim scores 2/3 accept) vs. fix (cheap) | REVIEWS.md R1 | pi-orchestrator | – |
