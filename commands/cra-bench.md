---
description: Run a fair interleaved benchmark and record evidence
argument-hint: <spec>
---

Use the experimenter agent with skills bench-protocol and log-to-evidence: $ARGUMENTS. Use crbench (lib/bench) for interleaved repeats and medians, capture the environment, add EVIDENCE.md rows, and produce Markdown + LaTeX tables.
